{
  "name": "overjs",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "scripts": {
    "start": "lerna run --parallel watch",
    "build": "lerna run build",
    "init": "lerna bootstrap && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
