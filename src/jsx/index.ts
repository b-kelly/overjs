import { JsxApplication } from "./JsxApplication";
import { JsxController } from "./JsxController";
import { createElement } from "./createElement";

// NOTE must use as `import jsx from "jsx";` so the compiler knows where jsx.createElement is from
export default { JsxApplication, JsxController, createElement };
